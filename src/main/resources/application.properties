spring.application.name=proy
server.port=8085

logging.level.org.springframework.web=INFO
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=INFO
logging.level.root=INFO

spring.output.ansi.enabled=ALWAYS

# URL de conexión (la obtienes de Supabase)
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=bd_proveedor

# Credenciales
spring.datasource.username=postgres
spring.datasource.password=proveedor-123-XD
spring.datasource.driver-class-name=org.postgresql.Driver

spring.sql.init.mode=never

# Configuración de Hibernate/JPA
spring.jpa.defer-datasource-initialization=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.flyway.enabled=true
spring.flyway.default-schema=bd_proveedor
spring.flyway.schemas=bd_proveedor
spring.flyway.locations=classpath:db/migration
